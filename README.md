# Connect Four

This web app utilizes socket.io and a React framework to allow two users to play connect four with eachother.

## How to Run:

1. Clone the repository/directory
2. In the project directory, run "npm install"
3. Run "npm run start" after the dependencies have been installed
4. Navigate to "http://localhost:4000
